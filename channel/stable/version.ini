[Lab]
Version=0.4.5
Notes=Fixes the 0.4.4 startup/runtime crash 'Failed to load DLL. Specifically: user32\' caused by two literal backslashes in the new gameplay UI guard DllCall function names; AutoHotkey strings do not use backslash escaping, so the Win32 separator must be a single backslash.|Adds a dedicated CI contract that scans every stable AHK module for doubled DLL/function separators and runs a real AutoHotkey v2 Win32 DllCall smoke test on Windows, because /Validate alone cannot detect this class of runtime error.|Retains the 0.4.4 gameplay isolation guard, deferred non-focus-stealing map capture, race-safe drag state, throttled editor rendering, exact local screenshots, true placement footprints/collision warnings, strategy-scoped Wiki portraits, layer filtering, safe saves/backups, Discord Remote, rewards, telemetry and F12 emergency stop.
