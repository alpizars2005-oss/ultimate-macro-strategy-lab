[Lab]
Version=0.4.8
Notes=Fixes a clean-install preflight regression introduced by the single-canvas map-module rename: preflight now verifies StrategyEditorMaps046.ahk, the same file shipped by files.map and included by StrategyEditorTab, instead of requiring the removed StrategyEditorMaps.ahk.|Adds an automated delivery contract that fails CI whenever Verify-LabModules asks for any file the stable channel does not actually deliver, preventing this class of installer-only failure from recurring.|Keeps the 0.4.7 pristine post-LoadGame screenshot boundary, exact Roblox CLIENT capture, hotbar-free editor projection, cyan/striped footprint model, post-run calibration, fast drag/pan renderer, tower portraits, safe saves/backups, Discord Remote, rewards, telemetry and emergency-stop behavior.
