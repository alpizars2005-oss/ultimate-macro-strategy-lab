[Lab]
Version=0.3.10
Notes=Hotfixes the clean 0.3.9 install warning where StrategyEditorUi constructed LabStratDocument but StrategyEditorTab forgot to include StrategyEditorCore in the real runtime include chain.|StrategyEditorCore is now explicitly included before StrategyEditorUi, so Main_Lab no longer depends on the CI harness accidentally providing the class.|Adds a dedicated Strategy Editor dependency-contract workflow that fails if LabStratDocument moves, disappears, or Core is not included before UI.|Keeps all 0.3.9 Editor fixes: canvas-owned marker visibility, synchronized Layer filtering, neutral separate footprints, single-HWND unique portraits, Stats, RewardLibrary/drop references, Discord Remote safe switching, encoding compatibility, safe saves/backups, map capture/calibration, telemetry and F12 emergency stop.
