[Lab]
Version=0.4.2
Notes=Hardens StartStrategy against stale/destroyed MainGui objects by checking the native HWND before Submit(false), preventing the Gui has no window crash during updater/reload transitions.|Adds automatic one-shot map capture for the screenshot-only Editor: after Ultimate Macro finishes normal or special-map camera alignment and CheckTheMapF, Strategy Lab captures the exact Roblox client view only when that map has no usable camera image cached.|Automatic map capture is gameplay-safe: an existing map returns immediately, failures are logged and swallowed, and a failed capture retries on a later run instead of interrupting the strategy.|Updates preflight for the 0.4 module set and installs both runtime hooks idempotently before syntax validation.|Retains the single-canvas editor, geometric layer filtering/drag, composited unit radii, local vanity badge, Stats lifecycle guard, Discord Remote, rewards, telemetry and F12 emergency stop.
