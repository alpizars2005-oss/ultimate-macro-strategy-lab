[Lab]
Version=0.2.16
Notes=Fixes the Windows preflight launch failure caused by passing %~dp0 with its trailing backslash inside a quoted PowerShell argument, which could surface as `Illegal characters in path` before AutoHotkey started.|run_lab.bat now changes into the install directory first and passes the quote-safe %CD% value to preflight.|lab_preflight.ps1 now independently normalizes legacy malformed quoted install paths, canonicalizes them with GetFullPath, validates the directory, and logs both raw and normalized values before touching Main_Lab.ahk.|The previous safe stray-Clear repair, automatic backups, 0.2.15 circular tower markers/placement-footprint rings, portrait presentation cache, calibrated map workflow and buffered editor rendering remain intact.
