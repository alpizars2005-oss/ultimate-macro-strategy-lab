[Lab]
Version=0.2.9
Notes=Editor now opens as a roomy 1000x690 workspace and automatically returns to the normal 700x565 size on other tabs.|The tactical canvas grows to 640x340, with a roomier 300px details panel; Expand uses a near-full 960x440 canvas.|Asset sync is strategy-scoped only, uses browser-like Fandom requests with PowerShell-to-curl fallback, and caches optimized local JPEGs (tower <=256px, map <=1280x720) in AppData.|Prevents accidental full-catalog syncs when no strategy is loaded, keeping downloads and macro size small.
