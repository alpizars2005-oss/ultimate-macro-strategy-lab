[Lab]
Version=0.2.6
Notes=Mouse wheel now zooms under the cursor and dragging empty map space pans the tactical viewport; dragging a tower still moves that placement.|Legacy pan-arrow buttons are disabled and removed from the active toolbar.|Asset sync now falls back to Windows curl.exe when Fandom rejects PowerShell requests, validates real image bytes and purges corrupt cached images.|Repairs stale loaded-state UI text after asynchronous refreshes.
