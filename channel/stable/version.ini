[Lab]
Version=0.4.9
Notes=Fixes the remaining 0.4.8 clean-install preflight failure by anchoring the pristine map screenshot to Ultimate Macro's REAL RunStrategy flow: immediately before activateTimescale(), after AlignCamera()/CheckTheMapF() camera-map verification and before ClickReady()/PlayStrategy()/RecordedSteps, so no tower can already be placed.|Replaces the incorrect test fixture that invented a LoadGame() call inside RunStrategy with a fixture matching the supplied clean Main.ahk ordering, and keeps the hook idempotent across repeated preflight runs.|Preflight now prints its exact exception and preflight.log path directly to the installer console on exit code 10, so future installer-only failures are immediately diagnosable.|Keeps exact Roblox CLIENT capture, hotbar-free editor projection, cyan/striped placement-footprint model, post-run calibration, fast drag/pan renderer, tower portraits, safe saves/backups, Discord Remote, rewards, telemetry and emergency-stop behavior.
