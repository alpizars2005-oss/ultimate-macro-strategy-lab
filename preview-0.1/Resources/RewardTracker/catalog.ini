; Strategy Lab preview reward catalog
; Template images intentionally match only the reward icon, not the xN quantity.
; The quantity is OCRed separately. If OCR is ambiguous, the reward is not counted.

[brown_crate]
display=Brown Crate
template=brown_crate.png
threshold=0.84

[duck_event]
display=Ducks
template=duck_event.png
threshold=0.84
